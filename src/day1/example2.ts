(()=> {
  let userName: string;
  userName = 'name';

  interface User {
    name: string;
    id: number;
  }

  const user: User = {
    name: 'name',
    id: 1
  }

  const user2: User = {
    name: 'name',
    id: 1
  }
})()
