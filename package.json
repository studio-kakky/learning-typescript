{
  "name": "typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc ./src/**/*.ts --outDir ./dist"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "typescript": "^4.0.5"
  }
}
